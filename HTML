HTML(HYPER TEXT MARKUP LANGUAGE)
    > HTML is the standard markup language for creating Web pages.
    >HTML describes the structure of a Web page.
    >HTML contains multiple tags for creating html file.
    
    
 HTML TAGS
   *They are used to define layouts for web pages.
   *<!DOCTYPE html> tells this document is html document.
   *Any html document contains two basic sections
            1)head-defines data about the document 
            2)body-contents of the web page
    *TITLE tag is used to hold text(Title of website).
    *META tag is used to define information abouy the file(character encoding).
